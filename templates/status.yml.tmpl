rep_id: {{rep_id}}
title: {{title}}
phase: scope          # scope | design | build | test | deploy | done | killed
# gate result values:
#   pending | approved | approved-with-noted-concerns | revise | aborted
gates:
  scope:   { result: pending }
  design:  { result: pending }
  build:   { result: pending }
  test:    { result: pending }
  deploy:  { result: pending }
iterations:
  scope:   0
  design:  0
  build:   0
  test:    0
  deploy:  0
created: {{created}}
updated: {{updated}}
